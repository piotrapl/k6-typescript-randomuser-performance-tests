{
  "name": "k6-typescript-randomuser-performance-tests",
  "version": "2.0.0",
  "description": "Simple k6 performance testing project using TypeScript and RandomUser API",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "npm run build && k6 run dist/randomUsers.test.js",
    "test:influx": "npm run build && k6 run --out influxdb=http://localhost:8086/k6 dist/randomUsers.test.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/k6": "^0.48.0"
  }
}