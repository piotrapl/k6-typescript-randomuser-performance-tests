{
  "name": "k6-reqres-performance",
  "version": "1.0.0",
  "description": "Simple k6 performance test using TypeScript",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && k6 run dist/users.test.js"
  },
  "devDependencies": {
    "@types/k6": "^0.48.0",
    "typescript": "^5.3.0"
  }
}
